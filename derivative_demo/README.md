# 导数与微分可视化 (Derivative & Differential Visualization)

这是一个经典的微积分概念可视化项目，生动演示了函数、切线、导数与微分之间的几何关系。

## 🧮 数学原理

本项目通过双坐标系联动，直观展示了以下核心数学概念：

### 1. 导数与斜率 (Derivative as Slope)
$$f'(x) = \lim_{\Delta x \to 0} \frac{\Delta y}{\Delta x} = \tan(\alpha)$$

- **原函数**: $f(x) = \sin(x)$ （上方蓝色曲线）
- **导函数**: $f'(x) = \cos(x)$ （下方红色曲线）
- **几何关联**: 原函数图像上任意点 $P$ 的切线斜率 $k$，恒等于导函数图像上对应点 $Q$ 的纵坐标值。即 $k_P = y_Q$。

### 2. 微分几何意义 (Geometric Meaning of Differential)
$$dy = f'(x)dx$$

- 在动画中的**绿色三角形**展示了微分的定义：
    - **底边**: 自变量的微小增量 $dx$（设为定值）。
    - **竖边**: 微分 $dy$。它代表了**沿着切线方向**的纵坐标增量。
- **视觉验证**: 当切线越陡峭（斜率绝对值越大），微分 $dy$ 越大；当切线水平（斜率为0）时，$dy$ 为 0。

## 🚀 功能特性

- **双视图联动**: 
    - 上方视图展示 $y=f(x)$ 及其切线运动。
    - 下方视图展示 $y'=f'(x)$，并通过虚线与上方同步，直观验证导数值与斜率的对应关系。
- **实时数据流**: 顶部面板实时显示从 $x$ 输入到 $f'(x)$ 计算，再到几何斜率 $k$ 的完整推导链。
- **交互控制**: 支持点击屏幕暂停/继续动画，便于观察特定状态（如波峰/波谷）。
- **高精度渲染**: 使用原生 SVG 绘制曲线，结合 **KaTeX** 渲染专业的 LaTeX 数学公式。
- **超慢速演示**: 动画周期设定为 80秒，确保每一个微小的斜率变化都能被清晰观察。
- **高画质录制**: 支持 4K 超采样录制 -> 1080p 输出，线条锐利，无锯齿。

## 🛠️ 技术栈

- **框架**: Vue 3 (Composition API)
- **样式**: Tailwind CSS
- **数学渲染**: KaTeX
- **动画**: Native SVG + Vue Reactivity (无第三方动画库)
- **录制**: Puppeteer + FFmpeg

## 📦 使用指南

### 安装与启动
```bash
npm install
npm run dev
```

### 视频录制
生成 1080p/60fps 的演示视频：

```bash
# 1. 启动服务（如果尚未运行）
npm run dev -- --port 5174

# 2. 运行录制脚本
npm run record
```
输出文件位于 `video_records/derivative_demo.mp4`。

```